[package]
name = "flowi"
version = "0.1.0"
edition = "2018"
build = "build.rs"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
bitflags = "2.0.0-rc.1"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }

[features]
static = []
dynamic = []

[workspace]
members = [
    "examples/rust/testbed",
]
