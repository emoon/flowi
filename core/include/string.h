#pragma once

#include "inline.h"
#include <string.h> // TODO: Configure strlen

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

typedef struct FlString {
    const char* string;
    int length;
} FlString;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

FL_INLINE FlString fl_str(const char* string) {
    return (FlString) { .string = string, .length = strlen(string) };
}
