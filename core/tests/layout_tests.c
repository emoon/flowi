#include "utest.h"
#include "../src/types.h"
#include "../include/layout.h"

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Generate a basic area

UTEST(Layout, area_default) {
	/*
	FlLayoutArea sidepanel = {
		.name = fl_str("sidepanel")
		.width = { .value = 300, .type = FlSizeType_Fixed },
	};

	FlLayoutArea header = {
		.name = fl_str("header")
	};

	FlLayoutArea main_layout = {
		.name = fl_str("main")
	};

	FlLayout full = {
		.size = { .x = 10, .y = 20, .width = 1024, .height = 512 },
	};

	FL_UNUSED(full);
	*/
}

